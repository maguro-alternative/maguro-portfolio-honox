{
  "name": "basic",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist .vercel/output && vite build --mode client && vite build && cp -r dist/assets .vercel/output/static/ && node scripts/fix-vercel-runtime.mjs",
    "build:cf": "rm -rf dist && vite build --mode client && DEPLOY_TARGET=cloudflare vite build && printf 'index.js\\n.vite/\\n' > dist/.assetsignore",
    "preview": "npm run build:cf && wrangler dev",
    "deploy": "npm run build && vercel deploy --prebuilt --prod",
    "deploy:cf": "npm run build:cf && wrangler deploy"
  },
  "private": true,
  "dependencies": {
    "@mdx-js/rollup": "^3.1.0",
    "@tsparticles/all": "^3.8.1",
    "@tsparticles/configs": "^3.8.1",
    "@tsparticles/engine": "^3.8.1",
    "highlight.js": "^11.11.1",
    "hono": "^4.7.10",
    "honox": "^0.1.40",
    "marked": "^18.0.5",
    "marked-highlight": "^2.2.4",
    "module": "^1.2.5",
    "vite-plugin-vercel": "^9.0.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "@eslint/js": "^10.0.1",
    "@hono/vite-build": "^1.3.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@tailwindcss/vite": "^4.1.11",
    "@types/node": "^24.3.0",
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "typescript-eslint": "^8.67.0",
    "vite": "^6.1.1",
    "wrangler": "^4.4.0"
  },
  "engines": {
    "node": "22.x"
  }
}
